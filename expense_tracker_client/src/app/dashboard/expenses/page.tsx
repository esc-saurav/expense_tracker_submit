import Expenses from "@/components/expenses/Expenses";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <Expenses />
    </>
  );
};

export default page;
